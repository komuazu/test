# 稼動日報 印刷実績ビューア — 作業メモ

アプリ本体は `kadou-web/`。使い方は [kadou-web/README.md](kadou-web/README.md)。
ここには**同じ間違いを繰り返さないための決めごと**だけを書く。

---

## 0. いちばん大事なこと

### 実データで確かめてから言う

このリポジトリの日報は **2013年〜今年の約490ファイル**あり、**年によって様式が違う**。
2ファイル見ただけで「この列は無い」「この位置にある」と決めつけないこと。

> **実際にやった間違い**
> 2014年と2020年の2ファイルだけを見て「出庫枚数は2019年より前の日報に無い」と
> 断定した。全ファイルを案件単位で数え直したら **2015年から入っていた**（2016年
> 以降は97〜99%）。この誤りを前提に「損紙率の分母に出庫枚数は使えない」と説明し、
> 分母を間違えたまま実装してしまった。

確かめ方は `web/data_<年>.json` を全年ぶんなめるのが速い。1ファイルずつ .xls を
開き直す必要はない。

```python
import json, glob
for p in sorted(glob.glob(r'web\data_*.json')):
    d = json.load(open(p, encoding='utf-8'))
    # d['records'][i]['det'] に日報の全列がそのまま入っている
```

### 用語の定義は憶測せず聞く

損紙率・予備率・稼働率は現場ごとに定義が違う。**分母を勝手に決めない。**
分母が変わると数字が1.5〜2倍変わる（損紙率は 3.19% ↔ 4.79% だった）。

### 元の .xls は絶対に書き換えない

`kadou_core.py` は読み込み前に必ず一時フォルダへコピーする。元フォルダへの
書き込み処理はこのリポジトリのどこにも存在させない。

---

## 1. 確定した計算式

`build.py` に実装済み。変えるときはここも直す。

```
稼働率   ＝ 有効時間の合計 ÷（22時間 × その月の稼働日数）
             稼働日 = 平日 − 祝日 − 会社の休業日（config.json の closed）
             22時間 = 昼勤11h＋夜勤11hの2直（build.py の SHIFT_HOURS）

出庫枚数 ＝ 実印刷枚数 ＋ 基準印刷予備          （倉庫から出した紙）
損紙率   ＝ やれ枚数     ÷ 出庫枚数            （出庫からはみ出して使った割合）
予備率   ＝ 基準印刷予備 ÷ 実印刷枚数          （本刷に対して持った予備の割合）
```

* **実印刷枚数** = 出庫枚数が入っている行の通し枚数。出庫が入らない行（同じ紙の
  2回目以降の刷り）まで足すと基準印刷予備が合わなくなる
* **基準印刷予備** = 出庫枚数 − 実印刷枚数。日報の「ＪＰ予備紙」列とほぼ同じだが、
  そちらは45%の行にしか無いので引き算で求める
* 実データで `出庫枚数 = 通し枚数 + ＪＰ予備紙` が **97.3%の行**で成立する

---

## 2. 稼動日報（.xls）の癖

年ごとに手で作り足されてきたので、**列の位置も有無も年でバラバラ**。

| 気をつけること | 中身 |
|---|---|
| **列の位置を決め打ちしない** | 「有効時間」のラベルは年によって **9・27・28・30・31列目**。値は**必ずラベルのすぐ右**。以前27列目固定だったため、2013〜2025年の日次集計ブロックを丸ごと取りこぼしていた |
| **同名の列が2つある** | `header_all` が2つめに「（集計）」を付ける。集計に使うのは**「（集計）」が付かない方＝本刷ブロック** |
| **日次集計ブロックに日付が無い** | 有効時間・準備合計などの行は日付欄が空。**すぐ上の明細行の日付を引き継ぐ**（`extract_sheet` の `last_day`） |
| **通し枚数は「延べ通し数」** | 4色4回通せば 4×枚数。**出庫枚数（実際の枚数）とは単位が違う**ので、割り算で混ぜない |
| **出庫枚数は用紙を出すたびに1行だけ** | 明細行の58.8%にしか無い。案件（管理番号）単位なら98.8%。集計は案件単位でそろえる |
| **出庫枚数は2015年から** | 2013・2014年は無し、2015年は58%、2016年以降97〜99%。無い年は率を出さず理由を出す |
| **明細行の判定を先に** | 「その他」など日次集計と同じ言葉が用紙銘柄に入ることがある。**日付＋数字の担当ｺｰﾄﾞが揃う行は明細**として扱い、そのあとで日次集計ブロックを見る |

### 母集団をそろえる

率を出すときは分子と分母を**同じ範囲**から取る。

> **実際にやった間違い**
> やれ枚数は全行から、出庫枚数は入っている行からだけ足したため、2014年の損紙率が
> **66429%** になった。いまは「出庫枚数のある案件」だけを分子・分母の両方で数え、
> 対象案件が半分未満の年は数字を出さないようにしている（`build_waste` の `cover`）。

---

## 3. 機械名とクライアント名

* **機械名**はファイル名から作る（`build.py` の `short_machine`）。年ごとに
  「新26・」と頭が変わるほか、**「新・三菱稼動日報」と「三菱稼動日報」が同じ機械**。
  頭の「新」は数字が付かないこともある。まとめ損ねると2015年のように**機械が8台**に
  なり、稼働率が半分（30.2%）になる
* **クライアント名**の名寄せは `ckey()`（NFKC＋空白除去）。`㈱` と `（株）` は
  同じになるが、**`金子園` と `（株）金子園` は別扱いのまま**
* **管理番号でクライアントをつなぐのは危険。** 1行の入力違いで別会社が連結される。
  実例: 管理番号 8712245 に「（株）平版印刷」と「（株）新藤慶昌堂」が混在しており、
  機械的につなぐと年間通し数の22%（6,268,427通し）が1社にまとまってしまう

---

## 4. テストの動かし方

```bash
python tests/core_test.py                                  # 124項目
python tests/make_fixture.py <一時フォルダ>
python server.py --src <一時フォルダ> --src /no/such/folder --no-browser &
python tests/ui_test.py                                    # 129項目
```

* **画面テストは `web/` を作り直す。** 必ず**フォルダごとコピーした場所**で動かす
* **`core_test.py` は途中で止まることがある。** `check_shared` が本物のフォルダ選択
  ダイアログを開いて人の操作を待つため（この作業より前から）。自動で流すときは
  `server.pick_folder` を差し替えてから `core_test.py` を実行する

  ```python
  import server; server.pick_folder = lambda initial=None: None
  ```

* **引数は README のとおりに渡す。** `--src /no/such/folder` はテストが
  この文字列そのものを探している。Windows だからと `C:\no\such\folder` に
  書き換えると失敗する

  > **実際にやった間違い**
  > 引数を勝手に書き換えたせいで1件落ち、それを「変更前から失敗している既知の
  > 不合格」と何度も報告してしまった。HEAD でも再現したのは同じ間違った引数で
  > 走らせていたからで、正しく渡せば **122 / 122 全部通る**。
  > テストが落ちたら、まず自分の走らせ方を疑うこと。

* **ダミー日報を置く一時フォルダの名前は何でもよい。** 以前は「設定に現在の
  フォルダが入る」の項目が名前に `sample` が入る前提になっていて、別の名前で
  作ると1件落ちた。いまは `/api/config` の中身と突き合わせているので、
  フォルダ名に依存しない（`srcAlt` の予備パスも画面に出るぶんだけ数える）
* ダミー日報（`make_fixture.py`）は数字を**割り切れる形**にしてある。
  有効時間8.0h／やれ＝通しの5%／基準予備＝通しの10% → 損紙率 4.55%、予備率 10.00%

---

## 4.5 記入欄（memo.json）は取りこぼさない

手入力欄は**打ち直しがきかない**。ここを触るときの決めごと。

| 決めごと | なぜ |
|---|---|
| **打った時点でブラウザに残す** | サーバーへは 0.4秒まとめて送る。その前にリロードされると、まとめ待ちの入力が丸ごと消えていた |
| **閉じぎわに送り切る** | `pagehide` / `beforeunload` / `visibilitychange` で `sendBeacon`。`fetch` はページが消えると途中で切れる |
| **送れたぶんだけ手元から外す** | 送信中に直した分を消さないため。届くまで `localStorage` の `kadouMemoPend` に持ったまま |
| **失敗を「保存しました」と言わない** | `r.ok` と `{ok:true}` を見る。以前は `.then(flash).catch(flash)` で、通信が落ちても成功表示だった |
| **消したことも送る** | 値 `null` で送る。手元から `delete` するだけだと、サーバーの差分マージに届かず**リロードで戻ってくる** |
| **1欄だけ直すときも3欄そろえて送る** | `MEMO[key(r)]` から作ると、部署名がずれている行で**触っていない2欄が空になる**。`memoKey(r)` で実際に入っているキーから作り直す |

**部署名がずれた記入欄の拾い直しは、いまの表に無い部署のときだけ。**
同じ月・同じ管理番号が**別の部署にも並ぶ行が実データに133件**ある
（2013年30件〜2026年1件）。無条件に管理番号だけで拾うと、隣の行の記入欄を
横取りしてしまう。`KEYSET`（いまの表にある目印の一覧）と照らしてから拾う。

> **実際に起きたこと**
> 「入力してリロードすると消える」という申し出を、実際に4通りとも再現できた。
> ①まとめ待ちの0.7秒以内にリロード ②部署ずれの行を1欄だけ直す
> ③消した欄が戻る ④保存に失敗しても成功表示。
> 1つ直して終わりにせず、**入力が消えうる道を全部たどること**。

---

## 5. 触らないもの・書き換えるときの作法

| ファイル | 扱い |
|---|---|
| 稼動日報の .xls | **読み取りのみ。** 一時フォルダへコピーしてから開く |
| `kadou-web/config.json` | 各PCの設定。勝手に書き換えない（`.gitignore` 済み） |
| `kadou-web/memo.json` | 画面で入力した記入欄。消さない |
| `kadou-web/web/*.json` | 起動のたび作り直される。消してよい |

* **改行は CRLF、文字コードは UTF-8**（`.bat` だけ **CP932**）。編集ツールで
  勝手に LF や UTF-8 にしないこと
* `setup.bat` と `セットアップ.bat`、`push.bat` と `プッシュ.bat` は**中身が同じ**。
  片方だけ直さない
* `セットアップ.bat` は取得済みフォルダの中で実行すると**そのフォルダ自身を更新**し、
  未コミットの変更があるときは**止まる**（`arunasiweb\arunasiweb` の入れ子を防ぐため）

---

## 6. 第三者検証（巻き戻り防止）

**自分の実装を自分で確かめただけで「できました」と言わない。**
このリポジトリでは、実装したのとは別のエージェントに検証させる。

```
Agent(subagent_type='nagumo-qa')  … 回帰リスク・異常値・境界の検証
Agent(subagent_type='yuuki-sa')   … 直し漏れ・書き換え漏れの洗い出し
Agent(subagent_type='kureha-te')  … E2E テストの追加・確認
```

### いつ必ず走らせるか

* 集計式（稼働率・損紙率・予備率・通し数）を足した／変えたとき
* `kadou_core.py` の抽出ルールを触ったとき
* 定義を**途中で変えた**とき（古い定義が別の場所に残りやすい）
* タブや表を足したとき

### 検証エージェントには本番フォルダで画面テストを走らせない

`ui_test.py` は `web/` を作り直し、`server.py` は `config.json` を書き換える。
**必ずフォルダごとコピーした場所と、空いているポート**で走らせること。

> **実際に起きた事故**
> 検証エージェントに「読み取り専用の調査」と伝えたが、`kadou-web` の中で
> `python server.py --port 8765` と `ui_test.py` を実行し、**本番の
> `config.json` と `web/data_*.json` を一時的に上書き**した（常駐している
> 本番サーバーと同じポート・同じ出力先だったため）。復旧はできたが、
> 依頼文に「本番フォルダでサーバーを起動しないこと」を書いていなかったのが原因。

> **もう一度やった事故**
> コピー先で動かしたのに、`%APPDATA%\kadou-web\memo.json`（記入欄の控え）は
> パスがコピーの外なので**本番のまま上書きした**。コピーしても逃げられない。
> `server.MEMO_HOME` を差し替えるか、`APPDATA` を一時フォルダに向けること。
> ただし `APPDATA` を向け直すと `xlwt` などのユーザー site-packages も
> 見えなくなるので、`core_test.py` では `server.MEMO_HOME` の差し替えを使う。

依頼文に次を必ず入れる。

```
テストを動かすときは kadou-web をどこかにコピーしてから、
8765 以外の空きポートで動かしてください。
kadou-web の中で server.py や ui_test.py を実行しないでください
（config.json と web/*.json が本番のまま上書きされます）。
記入欄の控え %APPDATA%\kadou-web\memo.json はコピーの外にあるので、
server.MEMO / server.MEMO_HOME を一時フォルダへ差し替えてから動かしてください。
```

### 検証エージェントに必ず伝えること

1. **「別のAIが実装したので鵜呑みにせず疑ってください」**と書く
2. 途中で定義やデータの形を変えたなら、**その履歴を全部伝える**
   （古い定義が残っている場所を探せるようにするため）
3. **実データ（`web/data_*.json`）で機械的に確かめてもらう。**
   ゼロ除算・100%超・負の値・NaN・空の年を全年ぶん探させる
4. 結果は **file:line と再現手順つき**で、問題が無い項目も
   「確かめて問題なし」と明記させる

### 直したあと

検証で見つかった問題を直したら、**もう一度同じ検証を走らせる**。
`core_test.py` と `ui_test.py` も通し直す。

---

## 6.5 印刷でつまずいたときに見るところ

「印刷できない・プレビューが出ない・落ちる」は、**紙の枚数**を先に疑う。

> **実際にあったこと**
> 記入欄の入力ボックス（textarea）が印刷でも高さを持ったままで、**1行36px**に
> なっていた。A4横1ページ（中身 718px）に**13行**しか入らず、実データ量の
> 月別明細1,500行が**117ページ**。プレビューが出てこないのは当然だった。
> 入力ボックスは紙では意味がないので、印刷の直前に中身を文字へ写して
> （`flattenFields`）、箱は印刷CSSで隠す。117 → 45ページになった。

* 行の多い表（`#detail` `#raw`）は印刷で `padding:1px 5px` に詰める
* それでも20ページを超えるときは、印刷前に枚数を出して確かめる（`BIG_PAGES`）
* 枚数は `page.pdf()` で測れる。1ページの中身は 1047×718px

```python
# 実データ量（1年6機械18,000行）のダミーで測る
pdf = pg.pdf(format='A4', landscape=True)
pages = pdf.count(b'/Type /Page')
```

### Excelファイルが印刷できないとき

`xlsx.py` は手で .xlsx を組み立てているので、**Excel が必ず書く部品**が
欠けやすい。欠けても開けるが、印刷（＝描画）でつまずく。

| 部品 | なぜ要る |
|---|---|
| `xl/theme/theme1.xml` | 配色とフォントの定義。印刷時の描画で参照される |
| `docProps/core.xml` `app.xml` | 文書情報 |
| ヘッダーのフォント指定 | `&"名前,字体"` と字体まで書く。ヘッダーは**印刷時に読まれる** |

確かめ方は、同じ内容を openpyxl で作って**部品の一覧を突き合わせる**のが速い。

```python
a = set(zipfile.ZipFile('当方.xlsx').namelist())
d = set(zipfile.ZipFile('openpyxl製.xlsx').namelist())
print(sorted(d - a))     # 当方に無い部品
```

**LibreOffice で印刷を試すなら `libreoffice-calc` が入っているか先に見る。**
入っていないと、どんな正しいファイルでも
`source file could not be loaded` になる。openpyxl 製のファイルも読めるか
確かめてから「再現した」と言うこと（一度これで誤報しかけた）。

---

## 6.6 2回目からの読み込みを省くしくみ

去年より前の日報は内容が決まっている。書き変わるのは当月ぶんだけなので、
**ファイルが変わっていない年は作り直さない**（実データ13年78ファイルで
起動 27秒 → 0秒、当月更新後 1.5秒）。

* 控えは `web/cache.json`。ファイルの目印（名前・大きさ・更新日時ナノ秒）と、
  年ごとに「どのファイルから作ったか」を持つ
* **更新日時は秒ではなくナノ秒で見る。** 秒までだと、日報を保存した直後に
  読み込んだときに変化を見逃す
* **新しく増えたファイルは、開くまでどの年か分からない。** そのため2段階で読む。
  ①変わった・増えたファイルを読んで年を知る ②その年に関わる残りを読む。
  これを1段階にすると、増えたファイルの年が作り直されず値が戻らない（実際に出た）
* **年をまたぐ情報は年のデータに入れない。** 読込元フォルダ・読み込み時刻は
  「今回の読み込み」の情報なので `years.json` に置き、画面は `runInfo()` で
  そちらを先に見る。`data_<年>.json` に入れると、作り直さない年で古いまま残る
* **出力の並びは決め打ちにする。** `daily` を読んだ順のまま出していたため、
  作り直す年があるかどうかで中身の並びが変わっていた

直したら必ず「少しずつ作り直した結果」と「全部読み直した結果」を突き合わせる
（`core_test` の `check_cache`）。速いだけで中身が違えば意味がない。

---

## 7. 画面を足すとき

タブは `年間サマリー／月別明細／得意先別／稼働率／損紙・予備／日報明細／元ファイル`。

* `index.html` にタブと `<section class="view" id="v-<名前>">` を足す
* `app.js` の `VIEWNAME` に日本語名を入れる（印刷の見出しに出る）
* 描き直しは `render()` から呼ぶ
* 表が長くなるときは印刷用に詰める（`@media print` の `padding:1px 5px`）。
  A4横1枚に収まるか、`page.emulate_media('print')` で高さを測って確かめる
  （1ページの中身は 1047×718px）
* データが無い年でも**落ちない**こと。理由を書いた案内を出す
